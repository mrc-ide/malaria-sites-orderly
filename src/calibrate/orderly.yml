script: script.R

parameters:
  country: ~
  year: ~

artefacts:
  - data:
      description: Intermediary outputs
      filenames:
        - stage_4.RDS

sources:
  - R/calibrate.R

depends:
  - process_country:
      id: latest(parameter:country == country && parameter:year == year)
      use:
        stage_3.RDS: stage_3.RDS
